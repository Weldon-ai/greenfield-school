# ===============================
# APPLICATION
# ===============================
spring.application.name=GreenFieldSchool

# ===============================
# DATABASE (Render Internal / Env Variables)
# ===============================
# Use DATABASE_URL provided by Render, or fallback to default
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://dpg-d6fsk4ogjchc73d2vi8g-a.singapore-postgres.render.com:5432/greenfield_school_db}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:greenfield_school_db_user}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:eOhSsPrCP3MSAptKIeYUmakzz1mvYEup}

# HikariCP Connection Pool Settings
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.connection-timeout=30000

# ===============================
# HIBERNATE / JPA
# ===============================
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.open-in-view=false
# Optional: remove explicit dialect, Hibernate will auto-detect from JDBC URL
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ===============================
# SERVER
# ===============================
server.port=${PORT:8080}

# ===============================
# THYMELEAF
# ===============================
spring.thymeleaf.cache=true

# ===============================
# FILE UPLOAD
# ===============================
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=200MB

# ===============================
# LOGGING
# ===============================
logging.level.org.springframework=INFO
logging.level.com.greenfield.sms=DEBUG

# ===============================
# WEATHER API
# ===============================
# Example: store your API key in an env variable


# ===============================
# SECURITY / ERROR HANDLING
# ===============================
server.error.include-message=never
server.error.include-stacktrace=never
management.endpoint.health.show-details=never